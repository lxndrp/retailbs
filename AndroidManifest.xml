<?xml version="1.0" encoding="utf-8"?>
<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  package="de.irf.it.retailbs.andometry"
  android:versionCode="1"
  android:versionName="1.0">

  <application
    android:icon="@drawable/icon"
    android:label="@string/app_name"
    android:debuggable="true">
    <activity
      android:label="@string/app_name"
      android:theme="@android:style/Theme.NoTitleBar"
      android:name="MainActivity"
      android:configChanges="orientation"
      android:screenOrientation="portrait">
      <intent-filter>
        <action
          android:name="android.intent.action.MAIN" />
        <category
          android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity
      android:name=".surface.SurfaceActivity"
      android:label="@string/a_surface_label"
      android:configChanges="orientation"
      android:screenOrientation="portrait" />
    <activity
      android:name=".chart.ChartActivity"
      android:label="@string/a_chart_label"
      android:configChanges="orientation"
      android:screenOrientation="portrait" />
    <activity
      android:name=".preferences.PreferencesActivity"
      android:label="@string/a_preferences_label"
      android:configChanges="orientation"
      android:screenOrientation="portrait" />
    <service
      android:name=".odometry.OdometryService"
      android:label="@string/s_odometry_label" />
  </application>

  <uses-sdk
    android:targetSdkVersion="8"
    android:minSdkVersion="8" />
</manifest> 
